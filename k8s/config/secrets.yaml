apiVersion: v1
kind: Secret
metadata:
  name: common-secrets
  namespace: microservices
type: Opaque
stringData:
  # Add your secrets here
  # Example:
  # jwt-secret: "your-secret-key"
  # db-password: "your-password"

