{{- if .Values.defaultDeployment.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-kube.fullname" . }}
  labels:
    {{- include "node-kube.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node-kube.selectorLabels" . | nindent 4 }}
{{- end }}
